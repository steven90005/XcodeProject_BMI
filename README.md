# XcodeProject_BMI

使用Xcode建立一個ios專案“BMI計算機”

教學概要：  
1.使用Xcode建立一個Single View Application  
2.在Main.storyboard(設計UI)中新建 
  (1)四個label(標題文字、身高文字、體重文字、顯示BMI文字)
  (2)兩個TextField(輸入身高、輸入體重)
  (3)一個Button(計算按鈕)
3.進入到ViewControllers.swift中建立Button觸發事件  
